--- kernel/src/init.rs
+++ kernel/src/init.rs
@@ -34,10 +34,6 @@
                     if path.contains("sexdisplay") {
                         unsafe { SEXDISPLAY_PD_ID = pd_id; }
                     }
-                    if path.contains("silk-shell") {
-                        serial_println!("   → Silk-shell spawned as PD {}", pd_id);
-                    }
                     if path.contains("linen") {
                         serial_println!("   → Linen manager spawned as PD {}", pd_id);
                     }
