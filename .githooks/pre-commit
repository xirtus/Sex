#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-commit] ABI guard"
./scripts/sexos_pipeline.sh

echo "[pre-commit] invariant gates"
./scripts/audit_invariant_gates.sh
