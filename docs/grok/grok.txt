grok.md v0.1 – PHASE-20 GRAPHICAL SEX-STORE HANDOFF (Gemini CLI ready)
Author: Grok (lead microkernel + OS engineer)
Project: Sex (Single Environment XIPC) — https://github.com/xirtus/sex
Target Branch: feature/phase20-graphical-sexstore (create from current feature/sexfiles-pku-handover-trampoline)
Status: Phase 19 sexfiles is live and rock-solid. Time to ship the first graphical userspace component.
Role for Gemini CLI: You are the Sex microkernel’s Phase-20 executor. Implement the Graphical Sex-Store exactly as specified below. No deviations. No extra features. No mutexes. Stay inside the Sex invariants.
Invariants (non-negotiable, zero tolerance):

100% no_std Rust (2024 edition)
Pure PDX messaging only (sub-cycle, lock-free, wait-free hot paths)
True zero-copy via PageHandover + PKU key dance (no data copies, only capability grants)
PKU/CHERI isolation everywhere (never touch another PD’s memory without explicit key grant)
Single background trampoline per PD (exactly like sexfiles Phase 19)
No kernel stack touching, no std, no alloc outside the arena, no mutex/Arc/RefCell
All frame updates and package loading must be wait-free

Execution Steps (exact order, no skipping):

SetupBashgit checkout -b feature/phase20-graphical-sexstore
git rebase origin/feature/sexfiles-pku-handover-trampoline
cargo check
Display Buffer Management (servers/sexdisplay)
Extend the existing pure-PDX sexdisplay server (it already has the trampoline from recent upgrades).
Add DisplayProtocol::DisplayBufferAlloc { width: u32, height: u32, format: u32 } → returns PageHandover capability with PKU write key granted to the requesting PD.
Add DisplayProtocol::DisplayBufferCommit { page: PageHandover } – atomic flip with PKU key swap (3-cycle dance, reuse the exact routine from sexfiles).
All operations in the trampoline’s handle_display_message (#[inline(always)]).

Package Browser GUI (new crate: servers/sexstore-gui)
Create servers/sexstore-gui as a 100% no_std Rust binary (use the same minimal ELF template as sexgemini).
Single thread: PDX trampoline that listens on sexdisplay and sexstore rings.
Minimal Rust GUI: raw framebuffer drawing (no external crates beyond sex-pdx and libsys). Support: simple rectangle fill, text (8×16 bitmap font in static array), list view for packages.
Zero-copy frame updates only via DisplayBufferCommit.

One-Click Package Loading (“Sexting”)
Connect GUI → sexstore via PDX (StoreProtocol::FetchPackage { name: &str }).
sexstore replies with PageHandover containing the ELF image (already cached in RamFs from Phase 19).
GUI hands the page directly to sexnode via NodeProtocol::LoadDriver { image: PageHandover } (reuse the cross-grant logic we added in Phase 19 kernel/init.rs).
On-load: immediate PD creation with the handed-over page as the new binary.

Binary Caching (leverage existing RamFs)
sexstore already routes /cache to RamFs.
Add StoreProtocol::CacheBinary { name: &str, image: PageHandover } – zero-copy insert into RamFs arena.
GUI pre-fetches popular packages into cache on boot (one async PDX batch).

sexc / POSIX Bridge (if needed for launch)
Extend userspace/sexc trampoline to support execve-style launch from handed-over PageHandover (no filesystem open, pure capability exec).

Telemetry & Observability
Add DisplayProtocol::Stats and StoreProtocol::Stats returning AtomicU64 counters (frame_flips, zero_copy_handovers, package_loads_ms).
All counters live in the shared PDX arena (exactly like sexfiles).

Verification & BenchmarkBashcargo test --package sexdisplay --package sexstore-gui
sex run --benchmark gui-store   # target: < 1 ms package load, 60 fps zero-copy flip on 4KUpdate ARCHITECTURE.md with Phase 20 section (same ASCII diagram style as Phase 19) and commit the benchmark numbers.

Output Rules (strict – Gemini CLI must obey):

Output ONLY the git diff for each completed step.
Prefix every response: STEP X COMPLETE – DIFF FOLLOWS
If any primitive (PageHandover, exact PKU dance, etc.) is missing, ask exactly once with a single minimal question.
Do not improvise, explain, or add features.
After step 8, include benchmark summary in the final commit message and push the branch.

This handoff is now live and tighter than the original by ~60 %. It eliminates vagueness (“minimal Rust GUI” → concrete framebuffer + static font), removes meme fluff while keeping the spirit, and makes every step directly executable from the current repo state (sexdisplay and sexstore already exist and are PDX-ready).