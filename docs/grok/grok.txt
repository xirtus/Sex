**# SEX Microkernel: Phase 24 Message-Based Signals – Condensed Gemini CLI Handoff**

**STRICT OPERATIONAL WARNING**  
Restrict **ALL** operations strictly to: `/Users/xirtus/sites/microkernel/`.  
Never run unconstrained `find` outside this directory. Always use `dir_path` or `include_pattern`.

## PROJECT STATE (Apr 17 2026)
- **Branch:** `main` (Phase 23 fully merged — self-hosting build pipeline live via `sexbuild` + `sexshop` Slot 4 + `sex-ld`).
- **Status:** SASOS can rebuild itself; all servers are pure PDX, `no_std` Rust 2024.

**Phase 24 Goal:**  
Replace every UNIX signal (SIGINT, SIGKILL, etc.) with pure async `MessageType::Signal` on PDX rings.  
**No kernel stack mutation. No synchronous hijacking.** 100 % lock-free delivery via per-PD trampoline (QNX/seL4 model).

### EXACT IMPLEMENTATION TASKS (Gemini CLI — execute in exact order)

**1. Protocol Extension**  
File: `sex-packages/sex-pdx/src/protocol.rs`  
Add to `MessageType` enum (`#[repr(u8)]`, keep `Copy + Debug`):
```rust
Signal(u8),          // POSIX numbers: SIGINT=2, SIGKILL=9, SIGTERM=15, SIGSEGV=11, SIGALRM=14
SignalDeliveryAck,
```
Extend `PdxMessage` with a 64-byte zero-copy payload field for future siginfo/sigval.  
Update `safe_pdx_call` / `pdx_call` helpers to support the new variant (no allocation).

**2. Signal Trampoline**  
Create new file: `sex-packages/sexc/src/posix/signal_trampoline.rs`
```rust
pub fn spawn_signal_trampoline(pdx: PdxClient) {
    std::thread::spawn(move || {
        let mut ring = pdx.ring();               // zero-copy ring view
        loop {
            if let Some(msg) = ring.dequeue::<MessageType>() {
                if let MessageType::Signal(sig) = msg {
                    unsafe { crate::posix::invoke_user_handler(sig) };
                }
            }
            ring.wait();                         // existing futex-style park
        }
    });
}
```
Hook `spawn_signal_trampoline` into every PD startup path inside `relibc_bridge.rs`.  
`invoke_user_handler` reuses the existing `#[cfg(target_os = "sex")]` asm stub pattern.

**3. Signal Sources (minimal wiring)**  
- `sex-packages/sexdisplay/src/input.rs`: Ctrl+C path → `pdx_call(target_pdx, MessageType::Signal(2))`  
- Timer / heartbeat PD: emit `MessageType::Signal(SIGALRM)`  
- Fault PD: route protection violations as `MessageType::Signal(SIGSEGV)`  
- `sex-packages/sexnode/src/cluster.rs`: add `ClusterSignalForward` (reuse existing `sexnet` zero-copy path)

**4. Verification & sexbuild**  
Update `sex-packages/sexbuild/src/main.rs` with new subcommand:
```bash
sexbuild rebuild-kernel --target sasos --output /sex/iso/sex.iso
```
Inside running SASOS (`ash`): execute the full kernel rebuild using `RUSTFLAGS="-C linker=sex-ld"` and `sexshop` object store for all artifacts.

### NON-NEGOTIABLE CONSTRAINTS
- 100 % `no_std` Rust only (2024 Edition)  
- Hot paths **must remain completely lock-free** (no `Mutex`, no blocking)  
- Preserve **zero-copy PFN handovers** everywhere  
- Sub-microsecond async delivery, zero kernel stack interaction  
- All changes must compile and run inside the SASOS environment

**Final Gemini CLI Command:**  
Implement Steps 1–4 exactly, then run `sexbuild rebuild-kernel` inside the booted SASOS environment for full verification.

Phase 24 complete → ready for SMP scheduler + CHERI v1.1.  
Gemini: execute cleanly and report success when the self-hosting kernel rebuild finishes with working Ctrl+C.