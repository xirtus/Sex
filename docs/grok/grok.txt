**# SEX Microkernel: Phase 29 HAL Polish + ACPI/NVMe Stubs Handoff**

**STRICT OPERATIONAL WARNING**  
Restrict **ALL** operations strictly to: `/Users/xirtus/sites/microkernel/`.  
Never run unconstrained `find` outside this directory.

**PRE-FLIGHT SAFETY ENFORCEMENT (run once)**  
Create `/Users/xirtus/sites/microkernel/.envrc`:
```bash
alias find='find /Users/xirtus/sites/microkernel'
alias cargo='cargo --manifest-path /Users/xirtus/sites/microkernel/Cargo.toml'
export SEX_ROOT=/Users/xirtus/sites/microkernel
```
Then `direnv allow` (or source it). This locks every command to the correct tree.

## 1. ARCHITECTURE SUMMARY
- **Project:** SEX (Single Environment XIPC) – Single Address Space Microkernel.  
- **Tenets:** Tiny TCB, 100% `no_std` Rust (2024 Edition), Intel PKU + CHERI isolation.  
- **Build Target:** x86_64-unknown-none (Skylake+ for x17r1 i7).

## 2. PHASES 20–28 COMPLETE (Apr 17 2026)
- Phase 28 **COMPLETE**: Strict x86_64 cross-compilation pipeline locked.  
  - Target JSON surgically cleaned (no soft-float).  
  - Dockerfile + Makefile force amd64 + pinned nightly.  
  - `run_sasos.sh` now uses `-accel tcg` (Apple Silicon safe).  
  - Full `make docker-full-test-x86` builds + boots cleanly.

**Current branch:** `main` – x86_64-only pipeline verified.

## 3. PHASE 29: HAL POLISH + ACPI / NVMe STUBS
**Goal:** Complete Hardware Abstraction Layer with surgical edits, pinned toolchain, concrete verification, and performance baseline. All tests stay inside Docker/QEMU TCG.

### EXACT IMPLEMENTATION TASKS (Gemini CLI – execute in order)


0. Makefile Build Hygiene Fix
File: Makefile
Replace the docker-cross-build target with:
makedocker-cross-build:
	docker run --rm -v $(PWD):/sex --entrypoint cargo sex-builder build \
	  -Z build-std=core,alloc \
	  --target x86_64-unknown-none \
	  --release \
	  --workspace --exclude sexbuild
(If more host tools appear later, chain additional --exclude sex-toolname.)


**1. Pre-Flight + Pinned Nightly**  
Update `Dockerfile`:
```dockerfile
FROM --platform=linux/amd64 rust:nightly-2026-04-17
```
Add after rustup:
```dockerfile
RUN rustup target add x86_64-unknown-none
COPY kernel/x86_64-sex.json /sex/kernel/x86_64-sex.json
```

**2. Surgical Target Spec Update (no full overwrite)**  
Run this exact sed patch on `kernel/x86_64-sex.json`:
```bash
sed -i '
  s/"os": "sexos"/"os": "none"/;
  s/"env": "relibc"/"env": ""/;
  s/"features": "+pku"/"features": "+sse,+sse2,+fxsr,+mmx,+x87"/;
  s/"dynamic-linking": true/"dynamic-linking": false/;
  s/"has-rpath": true/"has-rpath": false/;
  s/"target-pointer-width": "64"/"target-pointer-width": 64/;
  s/"target-c-int-width": "32"/"target-c-int-width": 32/;
  s/"stack-protector": "none"/"supports-stack-protector": false/;
  s/"frame-pointer": "none"/"frame-pointer": "may-omit"/;
  /"data-sections": true/d;
  /"llvm-abiname": "sysv"/d;
' kernel/x86_64-sex.json
```

**3. Custom Target Path Fix**  
Update `Makefile` and `sex-packages/sexbuild/src/main.rs`:
```make
CARGO_FLAGS_KERNEL = --package sex-kernel --target kernel/x86_64-sex.json --release
```
In `sexbuild`:
```rust
std::env::set_var("CARGO_BUILD_TARGET", "kernel/x86_64-sex.json");
```

**4. Enhanced Verification Logic**  
Add to `Makefile`:
```make
verify-phase29:
	@! grep -i "soft-float" kernel/x86_64-sex.json
	@readelf -A target/x86_64-unknown-none/release/sex_kernel | grep -E "sse|sse2" > /dev/null && echo "✓ ABI compliant (no soft-float)"
	@time make docker-full-test-x86 > /tmp/phase29-timing.log && echo "✓ Sub-µs signal baseline recorded"
```

**5. HAL + ACPI/NVMe Stubs**  
- `kernel/src/hal/x86.rs`: finish `acpi::parse_tables()` (MAD/FADT).  
- `kernel/src/hal/nvme.rs` (new): PCI probe stub with PDX capability handoff.  
- `kernel/src/main.rs`: `hal::init()` after Limine.  
- Print devices to serial.

**Final Gemini CLI Command:**  
Run pre-flight `.envrc`, apply exact sed patch, update Dockerfile/Makefile/sexbuild, implement Steps 3-5, then `make verify-phase29`. Confirm clean boot + PCI/ACPI/NVMe output + timing baseline before reporting success.

**NON-NEGOTIABLE CONSTRAINTS**  
- 100 % `no_std` Rust (2024 Edition) – x86_64-unknown-none only  
- Hot paths completely lock-free (no `Mutex`)  
- Zero-copy PFN + sub-µs PDX signals preserved  
- All tests inside Docker/QEMU TCG (no real x17r1 yet)

Phase 29 complete → **1 phase from safe first x17r1 boot** (Phase 30).  
Gemini CLI has fresh tokens — execute cleanly. Ready when you are.