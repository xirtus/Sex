Project: Sex (Single Environment XIPC) — https://github.com/xirtus/sex
Role: Gemini-side executor (Phase-19 sexfiles overhaul)
Target Branch: feature/sexfiles-pku-handover-trampoline (create from latest master)
Invariants (non-negotiable, zero tolerance):
100% lock-free/wait-free hot paths, sub-cycle PDX messaging, true zero-copy I/O via PKU handovers, PKU/CHERI isolation, single trampoline per PD only, no kernel stack touching ever, no mutexes/Arc/RefCell/std, no_panic + #![no_std] everywhere.
Execution Steps (exact order, no skipping, no additions):

Setup
git checkout -b feature/sexfiles-pku-handover-trampoline (rebase on master)
Trampoline Architecture (servers/sexfiles/src/)
Delete existing synchronous stub in main.rs
Create trampoline.rs using the exact PDX trampoline pattern from sexdrives/sexinput (manual ring-buffer loop, no extra threads)
Register with safe_pdx_register("vfs")
Route every VfsProtocol message to vfs::handle_vfs_message (#[inline(always)])

Zero-Copy PKU Handover (src/vfs.rs)
Extend VfsProtocol with HandoverRead { page: PageHandover, offset: u64, len: u32 } and HandoverWrite { page: PageHandover, offset: u64, len: u32 }
Implement the exact 3-cycle PKU dance: pku_grant_temporary(client_key, PKU_WRU) → capability copy only → pku_restore()
Large Read/Write must return PageHandover. Use arena allocator for metadata.

Lock-free LRU Cache (new file: src/cache.rs)
2Q lock-free LRU built on sex-pdx::AtomicRing
Inodes/dentries live in shared physical pages via one-time RegisterArena bump allocator at startup
Per-cacheline PKU keys, zero contention

Backend Registry (src/backends/)
Define FsBackend trait (exact signature from previous handoff)
Full lock-free RamFs (BTreeMap<u64, Inode> backed by arena)
Stub DiskFs and TmpFs (trait impl only)
Lock-free mount table (hashmap in trampoline context) for routing

sexc / relibc Bridge (userspace/sexc/src/posix/vfs.rs)
Extend existing VFS trampoline to support new Handover path
Map O_HANDOVER flag to zero-copy logic
Add posix_fadvise → VfsProtocol::PreWarmKeys hint

Capability Model
Wrap every InodeHandle in sex-pdx::Capability (rights encoded in capability bits)
Revocation = single PDX multicast RevokeKey

Telemetry
Add VfsProtocol::Stats returning AtomicU64 counters (ipc_ops_total, zero_copy_handovers, cache_hits, pku_flips) stored in shared arena


Output Rules (strict):

Output ONLY the git diff for each completed step.
Prefix every response with: STEP X COMPLETE – DIFF FOLLOWS
If any required primitive is missing, ask exactly once with a single minimal question.
Do not improvise, add features, explain, or deviate from invariants under any circumstances.
After step 8, run cargo test --package sexfiles && cargo bench --package sexfiles and include the benchmark summary in the final diff commit message. Update ARCHITECTURE.md Phase 19 section.

Begin execution immediately.