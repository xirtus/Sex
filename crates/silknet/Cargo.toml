[package]
name = "silknet"
version = "0.1.0"
edition = "2021"

[dependencies]
sex-pdx      = { path = "../sex-pdx" }
sex-graphics = { path = "../sex-graphics" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
