[package]
name = "sex-pdx"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", optional = true, default-features = false, features = ["derive", "alloc"] }
x86_64 = "0.15.4"
spin = "0.9.8"

[features]
default = []
serde = ["dep:serde"]

[lib]
